name: slack

on:
  workflow_dispatch:
    inputs:
      slack:
        description: "Enter a Slack message"
        required: true

jobs:
  slack:
    runs-on: self-hosted
    steps:
      - uses: ./.github/slacker
        with:
          SLACK_URL: ${{ secrets.SLACK_URL_TEST }}
          SLACK_MSG: ${{ github.event.inputs.slack }}
