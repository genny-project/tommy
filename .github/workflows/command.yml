name: command

on:
  workflow_dispatch:
    inputs:
      command:
        description: "Enter a command"
        required: true

jobs:
  command:
    runs-on: ubuntu-latest
    steps:
      - run: |
          ${{ github.event.inputs.command }}
